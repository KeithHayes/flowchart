st2=>start: punctuate_text(text)

op1=>operation: Build prompt with correction rules

op2=>operation: POST to self.api_url with prompt

cond1=>condition: response.status == 200?

cond1(no)->op3=>operation: Log warning and return original

cond1(yes)->op4=>operation: Extract corrected text

cond2=>condition: Is result empty?

cond2(yes)->op5=>operation: Log error and return original

cond2(no)->op6=>operation: Return stripped result

e2=>end: End punctuate_text

st2->op1->op2->cond1
cond1(no)->op3->e2
cond1(yes)->op4->cond2
cond2(yes)->op5->e2
cond2(no)->op6->e2
